apiVersion: v1
kind: Secret
metadata:
  name: tcm-api-keys
type: Opaque
data:
  # Note: In a real environment, these values would be base64 encoded
  # Example: echo -n "your-api-key" | base64
  deepseek-key: ${DEEPSEEK_KEY_BASE64} 